@using Sandbox;
@using Sandbox.UI;
@using VoxelWorld.Players;
@using VoxelWorld.Entities;

@namespace VoxelWorld.UI

@inherits PanelComponent
@implements ILocalPlayerListener

<root>
</root>

@code
{
	public void OnLocalPlayerCreated(Player player) => this.Panel.SetClass("enabled", true);
	public void OnLocalPlayerDestroyed(Player player) => this.Panel.SetClass("enabled", false);
}